const prettierConfig = {
  printWidth: 100, // Wrap lines at 100 characters for better readability
  tabWidth: 2, // Use 2 spaces per tab
  useTabs: false, // Prefer spaces over tabs
  semi: true, // Always add semicolons
  singleQuote: true, // Use single quotes for strings
  trailingComma: "all", // Add trailing commas where valid
  bracketSpacing: true, // Add spaces inside object brackets
  arrowParens: "always", // Always include parentheses around arrow function params
  endOfLine: "lf", // Use LF for consistency across OS
  jsxSingleQuote: true, // Use double quotes in JSX
  proseWrap: "preserve", // Preserve markdown formatting
};

export default prettierConfig;
